{% extends 'base.html.twig' %}

{% block title %} Connexion - Vacanows{% endblock %}

{% block main %}
<div class="centered-form">
    <div class="form-container">
        <h1>Connexion</h1>
        <form action="{{ path('app_login') }}" method="post">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="_username" value="{{ last_username }}" required>
            </div>
            <div class="form-group">
                <label for="password">Mot de passe:</label>
                <input type="password" id="password" name="_password" required>
            </div>

            {% if error %}
                <div class="error-message">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            <button type="submit">Se connecter</button>
        </form>
    <div class="toggle-form">
        <a href="{{ path('app_register') }}">Vous n'avez pas encore de compte ?</a><br>
        <a href="{{ path('app_forgot_password') }}">Mot de passe oublié ?</a>
    </div>
    </div>
</div>

<div class="wave"></div>
<div class="wave"></div>
<div class="wave"></div>
{% endblock %}
