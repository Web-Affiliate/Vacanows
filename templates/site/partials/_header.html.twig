<nav class="navbar navbar-expand-lg navbar-light amber navbar-general">
    <div class="container-fluid navbar-container">
        {% if content is not null %}
            <a class="navbar-brand mx-5 navbarC" href="{{ path('home') }}">
                {% if content.getLogo() is not null %}
                    <img src="{{ asset('uploads/images/' ~ content.getLogo()) }}" alt="logo" width="80" height="80" class="img-navbar">
                {% endif %}
            </a>
            <button class="navbar-toggler first-button navbar-toggler-icon" type="button" data-toggle="collapse" data-target="#navbarSupportedContent20"
                aria-controls="navbarSupportedContent20" aria-expanded="false" aria-label="Toggle navigation">
                <div class="animated-icon1"><span></span><span></span><span></span></div>
            </button>
            <div class="collapse navbar-collapse navbar-container" id="navbarSupportedContent">
                <ul class="navbar-nav mb-2 mb-lg-0">
                    {% for i in 1..4 %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('guides', {'guideSlug': guides[i-1].slug}) }}">{{ attribute(content, 'getNavbar' ~ i) }}</a>
                        </li>
                    {% endfor %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('about-us') }}">{{ content.getNavbar5 }}</a>
                    </li>
                </ul>
                <a class="btn ml-auto contact-button" href="#" title="Contactez nous" rel="nofollow" data-bs-toggle="offcanvas" data-bs-target="#offcanvasContactForm" aria-controls="Contactform" data-action="OuverturefromContactform" data-label="Nous contactez" data-categorie="Header" data-value="1">Contact</a>
            </div>
        {% endif %}
    </div>
</nav>


{% include 'site/form/formContact.html.twig' %}



<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script>
    $(document).ready(function() {
        $('.navbar-toggler').on('click', function() {
            $(this).toggleClass('active');
            $('.navbar-collapse').slideToggle('slow');
            $('.animated-icon1').toggleClass('open');
        });

        $('.navbar-nav .nav-link').on('click', function() {
            $('.navbar-nav .nav-link').removeClass('activenav');
            $(this).addClass('activenav');
        });

        $(window).scroll(function() {
            if ($(this).scrollTop() > 100) {
                $('.navbar-general').addClass('navbar-container-scrolled');
            } else {
                $('.navbar-general').removeClass('navbar-container-scrolled');
            }
        });

    });

    
</script>
